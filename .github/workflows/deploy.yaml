name: Deploy to AWS

on:
    workflow_dispatch:
    push:
        branches:
          - main

env:
    AWS_REGION: eu-central-1
    AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
    AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
    PRIVATE_SSH_KEY: ${{ secrets.PRIVATE_SSH_KEY }}
    PUBLIC_IP: ${{ secrets.PUBLIC_IP }}
    OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
    DOCKER_REPOSITORY: movie-rag
    MOVIE_TAG: movie-${{ github.ref_name }}

jobs:
    publish-to-ecr:
        runs-on: ubuntu-latest

        steps:
          - name: Checkout code
            uses: actions/checkout@v2

          - name: Login to AWS ECR
            id: login-ecr
            uses: aws-actions/amazon-ecr-login@v1
          
          - name: Build and push movie docker image
            env:
                REGISTRY: ${{ steps.login-ecr.outputs.registry }}
            run: |-
                # Delete old movie image if exists
                aws ecr batch-delete-image --repository-name $DOCKER_REPOSITORY --image-ids imageTag=$MOVIE_TAG --region $AWS_REGION || true
                
                # Build and push movie
                docker build -f Docker/Dockerfile -t $REGISTRY/$DOCKER_REPOSITORY:$MOVIE_TAG .
                docker push $REGISTRY/$DOCKER_REPOSITORY:$MOVIE_TAG



    pull-from-ecr-and-deploy:
        needs: publish-to-ecr
        runs-on: ubuntu-latest

        steps:
          - name: Login to AWS ECR
            id: login-ecr
            uses: aws-actions/amazon-ecr-login@v1
          
          - name: Deploy docker images to EC2
            env:
              REGISTRY: ${{ steps.login-ecr.outputs.registry }}
            uses: appleboy/ssh-action@master
            with:
              host: ${{ env.PUBLIC_IP }}
              username: ec2-user
              key: ${{ env.PRIVATE_SSH_KEY }}
              envs: PRIVATE_SSH_KEY, REGISTRY, DOCKER_REPOSITORY, MOVIE_TAG, AWS_ACCESS_KEY_ID, AWS_SECRET_ACCESS_KEY, AWS_REGION, OPENAI_API_KEY
              command_timeout: 15m
              script: |-
                echo "Deploying MovieRAG Docker images to EC2..."
                
                # Login to ECR
                echo $(aws ecr get-login-password --region $AWS_REGION) | sudo docker login --username AWS --password-stdin $REGISTRY
            
                # Stop and remove old containers
                sudo docker stop movie-rag || true
                sudo docker rm movie-rag || true
                sudo docker system prune -a -f
            
                # Pull new Docker image
                sudo docker pull $REGISTRY/$DOCKER_REPOSITORY:$MOVIE_TAG

                # Create data directory for persistent storage
                sudo mkdir -p /home/ec2-user/data
                
                # Run movie container (MovieRAG web server)
                sudo docker run -d \
                  --name movie-rag \
                  --restart=unless-stopped \
                  -e OPENAI_API_KEY=$OPENAI_API_KEY \
                  -p 5005:5005 \
                  -v /home/ec2-user/data:/app/data \
                  $REGISTRY/$DOCKER_REPOSITORY:$MOVIE_TAG
              